
import { housePropertyType } from 'bayzat/helpers/house-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | house property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = housePropertyType([42]);
  assert.ok(result);
});

